@using Cv.Mvc.Project.Models
@model List<TblReferans>

<div class="container-fluid py-5" id="testimonial">
    <div class="container">
        <div class="position-relative d-flex align-items-center justify-content-center">
            <h1 class="display-1 text-uppercase text-white" style="-webkit-text-stroke: 1px #dee2e6;">Referanslar</h1>
            <h1 class="position-absolute text-uppercase text-primary">Referanslarım</h1>
        </div>
        <div class="row justify-content-center">
            <div class="col-lg-8">
                @foreach (var item in Model)
                {
                    <div class="owl-carousel testimonial-carousel">
                        <div class="text-center">
                            <i class="fa fa-3x fa-quote-left text-primary mb-4"></i>
                            <img class="img-fluid rounded-circle mx-auto mb-3" src="/Template/CvTemp/img/MuratYücedağ.jpeg" style="width: 80px; height: 80px;">
                            <h3 class="font-weight-bold m-0">@item.Adsoyad</h3>
                            <h5 class="font-weight-bold m-0">@item.Title</h5>
                            <h6 class="font-weight-bold m-0">@item.Email</h6>
                            <span>@item.Telefon</span>
                        </div>

                    </div>
                }

            </div>
        </div>
    </div>
</div>
